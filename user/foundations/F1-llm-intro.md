# F1: Intro to Large Language Models — Karpathy

> **来源：** Andrej Karpathy YouTube (2023.11) · ~1h
> **一句话：** 用 1 小时让你从"知道 ChatGPT 能聊天"变成"理解 LLM 整个技术栈"。

---

## ⚡ 3 分钟速读版

```
一句话: LLM 本质是一个压缩了互联网知识的文件，通过 next-token prediction 工作。
3 个关键概念: Token预测机制 / 训练三阶段 / LLM 的能力边界
对 Muse 最重要的: Muse 的所有 Agent 行为（Planning/Tool Use）都建立在 token 预测之上
面试必记: "LLM 不是在'思考'，是在预测下一个最可能的 token"
```

---

## 🔬 核心原理（三栏表）

| 概念 | 能力来源（模型怎么获得的） | 激活方式（工程上怎么触发） | 类比（仅类比） |
|------|------------------------|------------------------|--------------| 
| **Next-token prediction** | 在海量文本上训练：给定前 N 个 token，预测第 N+1 个 | 自回归生成：每次预测一个 token，追加到序列末尾，再预测下一个 | 仅类比：像"接龙游戏"，但每次只接一个字 |
| **Transformer** | 架构创新（Vaswani 2017）：Self-Attention 让模型看到所有位置的信息 | 模型前向传播时，每层 attention 计算 token 之间的关联权重 | 仅类比：像一个教室里每个学生都能直接和其他任何学生交流 |
| **参数 = 压缩的知识** | 预训练阶段：10TB 文本压缩成 ~140GB 参数文件（LLaMA 70B） | 参数是固定的权重矩阵，推理时不变 | 仅类比：像把整个维基百科"蒸馏"成一个文件 |

---

## 1. LLM 到底是什么？

### 1.1 两个文件

Karpathy 的开场非常直接：

> 一个 LLM 就是两个文件：
> 1. **参数文件**（parameters）— 一堆浮点数，比如 LLaMA 70B 就是 140GB
> 2. **运行代码**（run file）— 大约 500 行 C 代码就能跑起来

**这意味着什么？**
- LLM 可以完全离线运行（不需要网络）
- 参数文件 = 从训练数据中"压缩"出来的知识
- 运行代码做的事就是：给定已有的 token 序列，预测下一个 token

### 1.2 Next-Token Prediction 的深层含义

> [!IMPORTANT]
> **预测下一个 token ≠ 简单的自动补全。**
> 如果模型能准确预测任何文本的下一个 token，它必须"理解"文本的含义。
> 比如预测一道数学题答案的下一个数字，模型必须"会做"这道题。

```
输入: "法国的首都是"
       ↓ 模型计算所有可能 token 的概率
预测: "巴" (概率 0.95) > "柏" (概率 0.01) > ...
       ↓ 采样（可能是 top-p / temperature 控制）
输出: "巴"
       ↓ 追加到序列
输入: "法国的首都是巴"
       ↓
预测: "黎" (概率 0.99)
...
```

**为什么这这么强大？**

要准确预测下一个 token，模型被迫在参数中编码：
- 语法规则（"is" 后面该跟什么词性）
- 事实知识（法国首都是巴黎）
- 推理能力（数学题的答案是什么）
- 编程能力（这段代码下一行该写什么）

> Karpathy: "Next-token prediction is a deceptively simple objective, but it turns out to be extremely powerful."

---

## 2. 训练三阶段

### 2.1 Stage 1: 预训练（Pretraining）

| 维度 | 细节 |
|------|------|
| **数据** | ~10TB 文本（互联网爬虫 + 书籍 + 代码 + 维基百科） |
| **计算** | 数千 GPU × 数周 = 数百万美元 |
| **目标** | Next-token prediction（自监督） |
| **产出** | **Base Model** — 能写文本，但不会对话，不听指令 |

**Base Model 的特征：**
- 给它 "法国的首都是"，它会接 "巴黎"
- 给它 "请帮我写一封邮件"，它可能会接 "请帮我写一份报告" 而不是真的写
- 它不知道自己是一个"助手"，它只是一个文本补全器

### 2.2 Stage 2: 监督微调（SFT — Supervised Fine-Tuning）

| 维度 | 细节 |
|------|------|
| **数据** | ~10K-100K 高质量对话样本（人类写的 Q&A） |
| **目标** | 让 Base Model 学会"问答"格式 |
| **产出** | **SFT Model** — 会对话了，但可能说有害内容 |

**SFT 做了什么？**
```
训练样本:
  User: "什么是光合作用？"
  Assistant: "光合作用是植物利用光能将二氧化碳和水转化为葡萄糖和氧气的过程。"

模型学会: 看到 "User:" 后面的问题 → 生成 "Assistant:" 后面的回答
```

> [!IMPORTANT]
> **SFT 改变的是模型的"行为模式"，不是"知识量"。** 知识在预训练时已经有了，SFT 只是教模型怎么用对话格式来展现知识。

### 2.3 Stage 3: RLHF（Reinforcement Learning from Human Feedback）

| 维度 | 细节 |
|------|------|
| **数据** | 人类对多个回答的偏好排序 |
| **方法** | 训练 reward model → 用 PPO 优化生成策略 |
| **产出** | **RLHF Model** — 更有帮助、更安全、更符合人类偏好 |

**RLHF 的流程：**
```
1. 给模型同一个问题，让它生成多个回答 (A, B, C, D)
2. 人类标注员排序: B > D > A > C
3. 用这些排序训练一个 Reward Model（打分器）
4. 用 Reward Model 指导主模型优化（PPO 算法）
5. 结果：模型生成的回答越来越符合人类偏好
```

---

## 3. LLM 的能力边界（面试重点）

### 3.1 LLM 能做什么

| 能力 | 来源 | 注意 |
|------|------|------|
| 语言理解和生成 | 预训练 | 核心能力 |
| 知识问答 | 预训练（编码在参数中） | 有截止日期，可能过时 |
| 代码生成 | 预训练（训练数据含代码） | 能力强但不保证正确 |
| 推理（CoT） | 预训练 + prompt 激活 | 不是"真推理"，是模式匹配 |
| 遵循指令 | SFT 阶段训练 | 核心行为 |
| 工具使用 | SFT 阶段的工具调用样本 | Function Calling |
| 安全对齐 | RLHF 阶段 | 可被越狱 |

### 3.2 LLM 不能做什么

| 限制 | 原因 | 怎么绕过 |
|------|------|---------|
| **不会真的"想"** | 每个 token 的计算量是固定的（1 次前向传播） | CoT: 通过多 token 输出来分配更多计算 |
| **会幻觉** | 训练目标是"看起来像真的"，不是"是真的" | RAG: 用检索结果约束 |
| **有知识截止日** | 参数在训练后固定 | Tool Use: 调搜索引擎 |
| **上下文有限** | Transformer 的 O(n²) 注意力 | 压缩/摘要/外部存储 |
| **不是确定性的** | 采样有随机性（temperature） | temperature=0 近似确定性 |

> [!IMPORTANT]
> **面试回答模板：** "LLM 本质是一个通过 next-token prediction 训练的神经网络。它不是在'思考'，而是在基于训练数据的统计规律预测最可能的下一个 token。它的'推理能力'是通过 prompt 引导（如 CoT）让模型在输出中花更多 token 来分解问题，从而提高准确率。"

---

## 4. DeepSeek R1 / OpenAI o1 — 思考是怎么实现的？

> **发散：** Day 01-07 会反复遇到"模型的推理能力"，这里提前打好基础。

| 概念 | 能力来源 | 激活方式 | 类比（仅类比） |
|------|---------|---------|--------------|
| **CoT (Chain-of-Thought)** | 预训练语料中包含推理链文本（数学解题过程、编程步骤说明） | Prompt: "Let's think step by step" 或 few-shot 示例 | 仅类比：像考试写解题过程 |
| **DeepSeek R1 思考** | 在 RL 阶段，reward 函数奖励"先推理再回答"的行为 → 模型自动学会输出 `<think>` 标签 | 模型自动在回答前产出思考过程（不需要 prompt 引导） | 仅类比：像考试前打草稿（但机制完全不同——是 RL 训练出来的） |
| **OpenAI o1 推理** | Test-time compute scaling: 在推理阶段投入更多计算（生成更长的思考链） | 系统自动分配推理预算，模型生成隐式推理链 | 仅类比：像给考生更多答题时间 |

**CoT 为什么能提高准确率？**

```
不分步 (直接回答):
  Q: 15 × 17 = ?
  模型只做 1 次前向传播来预测答案 → 很可能错

分步 (CoT):
  Q: 15 × 17 = ?
  Step 1: 15 × 10 = 150    ← 1 次前向传播，简单，对
  Step 2: 15 × 7 = 105     ← 1 次前向传播，简单，对
  Step 3: 150 + 105 = 255  ← 1 次前向传播，简单，对
  
每步只做简单计算 → 每步都更可能对 → 最终答案更可能对
```

> [!IMPORTANT]
> **CoT 的本质：** 把一个需要大量计算的问题拆成多个简单子问题，每个子问题用一次前向传播就够了。这不是模型"学会了思考"，而是"用更多 token 来分配更多计算"。

---

## 🎯 Muse 映射

| Karpathy 讲的 | Muse 里对应的 |
|--------------|-------------|
| Next-token = LLM 的唯一能力 | Muse 所有行为都建立在此之上 |
| SFT 教会工具调用 | MCP 工具（notify_planner, memory） |
| RLHF 教会遵循偏好 | 模型遵循 system prompt 里的人格/约束 |
| 上下文有限 | OpenCode compaction 压缩机制 |
| temperature 控制 | Workflow 场景用低 temperature，聊天用高 |

---

## 💼 面试必答

**Q: LLM 是怎么工作的？**
> 本质是 next-token prediction。在海量文本上预训练，学会预测任意文本序列的下一个 token。然后通过 SFT（监督微调）学会对话格式，通过 RLHF 学会符合人类偏好。

**Q: DeepSeek R1 的"思考"是怎么实现的？**
> 不是 prompt 引导的 CoT，而是在 RL 训练阶段，reward 函数奖励模型在回答前先输出推理过程。模型自动学会了在 `<think>` 标签里做推理。本质是 test-time compute scaling — 在推理时花更多 token（计算）来提高准确率。

**Q: CoT 为什么有效？**
> 把一个复杂问题拆成多个简单子问题，每个子问题只需一次前向传播。每步对的概率高 → 链式下来最终答案更可能对。不是模型"学会思考"，是"用更多 token 分配更多计算"。
